{
  "name": "edge-pair-pc-server",
  "private": true,
  "scripts": {
    "setup": "python3 -m venv .venv && .venv/bin/pip install -r requirements.txt",
    "dev": ".venv/bin/uvicorn app.main:app --reload --host 0.0.0.0 --port 18124",
    "start": ".venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 18124"
  }
}
